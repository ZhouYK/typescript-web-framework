import React, {FC, PropsWithChildren} from 'react';

interface Props {

}

interface Staff {
  id: number;
  name: string;
  from: number;
  to: number;
}

const Flow: FC<Props> = (props: PropsWithChildren<Props>) => {

}

export default Flow;
